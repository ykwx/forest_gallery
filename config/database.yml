############################################
####  Postgres Database config          #### 
####  Change the database name          ####
####  to match your configuration       ####  
############################################

development:
 adapter: postgresql
 database: forestgallery-dev
 host: localhost
 username: jonathan
 password: 
 pool: 5
 timeout: 5000

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
 adapter: postgresql
 database: forestgallery-test
 host: localhost
 pool: 5
 timeout: 5000

production:
 adapter: postgresql
 database: forestgallery-prod
 host: localhost
 pool: 5
 timeout: 5000
 
